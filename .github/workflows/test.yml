name: Go contracts

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: contracts-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      matrix:
        go: ['1.25.8', '1.26.1']
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: ${{ matrix.go }}
          cache: false
      - name: Format
        run: test -z "$(gofmt -l main.go cmd internal)"
      - run: go vet ./...
      - run: go test -race -cover ./...
      - run: go build ./...

  terraform-protocol:
    strategy:
      matrix:
        terraform: ['1.14.0', '1.14.2']
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: '1.25.8'
          cache: true
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: ${{ matrix.terraform }}
          terraform_wrapper: false
      - name: Test protocol with synthetic API fixtures only
        env:
          IWINV_PROTOCOL_TEST: '1'
        run: TF_ACC_TERRAFORM_PATH="$(command -v terraform)" go test -race ./internal/provider -run TestProtocol -v
      - name: Validate examples with the actual provider binary
        run: |
          go build -o bin/terraform-provider-iwinv .
          terraform fmt -check -recursive examples
          cat > "$RUNNER_TEMP/iwinv.tfrc" <<EOF
          provider_installation {
            dev_overrides {
              "dokdo2013/iwinv" = "$GITHUB_WORKSPACE/bin"
            }
            direct {}
          }
          EOF
          TF_CLI_CONFIG_FILE="$RUNNER_TEMP/iwinv.tfrc" terraform -chdir=examples/data-sources/iwinv_availability_zones validate
